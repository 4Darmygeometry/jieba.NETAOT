name: Build, Test, and Publish

on:
  push:
    branches: [ master, main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ master, main ]

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  NUGET_XMLDOC_MODE: skip

jobs:
  build-and-test:
    runs-on: windows-latest
    strategy:
      matrix:
        targetFramework: [ net10.0, netstandard2.0, net48 ]
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore -c Release -f ${{ matrix.targetFramework }}

      - name: Run tests (net10.0 only)
        if: matrix.targetFramework == 'net10.0'
        run: dotnet test test/Segmenter.Tests/Segmenter.Tests.csproj --no-build -c Release -f net10.0 -v normal

  aot-test:
    runs-on: windows-latest
    needs: build-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Publish AOT
        run: dotnet publish test/AotTest/AotTest.csproj -c Release -r win-x64

      - name: Run AOT test
        run: test/AotTest/bin/Release/net10.0/win-x64/publish/JiebaNet.AotTest.exe

  netframework-test:
    runs-on: windows-latest
    needs: build-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Build .NET Framework 4.8 test
        run: dotnet build test/NetFrameworkTest/NetFrameworkTest.csproj -c Release

      - name: Run .NET Framework 4.8 test
        run: test/NetFrameworkTest/bin/Release/net48/JiebaNet.NetFrameworkTest.exe

  publish-nuget:
    runs-on: windows-latest
    needs: [ build-and-test, aot-test, netframework-test ]
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Extract version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Build all projects
        run: |
          dotnet build src/Segmenter/Segmenter.csproj -c Release
          dotnet build src/Analyser/Analyser.csproj -c Release

      - name: Pack jieba.NETAOT
        run: |
          msbuild -t:Pack -p:Configuration=Release -p:Version=${{ steps.get_version.outputs.VERSION }} src/jieba.NETAOT/jieba.NETAOT.csproj
          mkdir -p artifacts
          cp src/jieba.NETAOT/bin/Release/jieba.NETAOT.*.nupkg artifacts/

      - name: Publish to NuGet.org
        run: dotnet nuget push artifacts/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
